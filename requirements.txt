# ============================================================
# MythCoder - 本地自主 AI 编程智能体 (CLI Agent)
# Python 3.10+
# ============================================================

# --- 大模型接入 ---
litellm>=1.40.0           # 统一 LLM 接口，支持 OpenAI / Claude / 本地模型
openai>=1.30.0            # OpenAI 官方 SDK（LiteLLM 底层依赖）
tiktoken>=0.7.0           # Token 计数，用于上下文窗口管理

# --- CLI 终端交互 ---
rich>=13.7.0              # 终端美化：Markdown 渲染、面板、颜色、进度条
prompt-toolkit>=3.0.43    # 高级输入处理：多行编辑、自动补全、历史记录
pygments>=2.17.0          # 代码语法高亮（Rich 依赖）

# --- 配置解析 ---
pyyaml>=6.0               # YAML 配置文件解析
pydantic>=2.5.0           # 配置数据校验与模型定义

# --- 工具依赖 ---
pathspec>=0.12.0          # .gitignore 规则匹配，用于文件搜索过滤

# --- 通用工具 ---
colorama>=0.4.6           # 跨平台终端颜色支持（Windows 兼容）
